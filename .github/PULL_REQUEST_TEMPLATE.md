## What this changes

<!-- A sentence or two. Link the issue it fixes, if there is one: Fixes #123 -->

## Why

<!-- What problem it solves. Skip if it's obvious from the above. -->

## How it was tested

<!-- Which macOS version, and which app you drove if the change touches execution. -->

- macOS version:
- Tested against application:

## Checklist

- [ ] `swift build` and `swift test` pass.
- [ ] New or changed public API has a `///` doc comment describing purpose, parameters and return value.
- [ ] Docs updated if behaviour, parameters or return values changed.
- [ ] Tests added for anything deterministic (escaping, substitution, parsing).
- [ ] No breaking change to public API — or it's called out here, for a major release.
- [ ] Package messages stay in English.
